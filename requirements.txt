# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.11.0
pydantic-settings>=2.7.0

# OpenAI SDK
openai>=2.0.0

# Twilio
twilio>=9.1.0

# Audio Processing (for voice)
websockets==12.0
pydub==0.25.1

# FastAPI form parsing (Twilio webhooks)
python-multipart==0.0.9

# Utilities
python-dotenv==1.0.0
httpx==0.26.0
aiofiles==23.2.1
tenacity==9.0.0
pytz==2025.2

# Google Calendar & Gmail (optional; install separately if needed)
google-api-python-client==2.120.0
google-auth==2.27.0
google-auth-oauthlib==1.2.0

# Calendar (ICS) parsing for non-Google calendars
ics==0.7.2
requests>=2.31.0

# Outlook/Microsoft Graph (optional; install separately if needed)
msal>=1.24.0

# Database ORM
sqlalchemy>=2.0.36
alembic==1.14.0
psycopg2-binary>=2.9.9

# Postgres driver (Neon / DATABASE_URL) is optional; see notes in src/database/database.py

# Contact parsing (vCard support)
vobject==0.9.6.1

# Rate Limiting
slowapi==0.1.9

# Retry Logic (same package; keep single pinned version above)

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-mock==3.14.0
