### Core
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000
FRONTEND_URL= # e.g. https://your-app.vercel.app (Optional, defaults to Referer header)

### Auth (recommended for any non-local use)
# If set, all /api/* endpoints (except /api/health* and /api/*/oauth/*) require this token:
# - X-Godfather-Token: <token>
# - or Authorization: Bearer <token>
GODFATHER_API_TOKEN=change-me

### OpenAI
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o
OPENAI_REALTIME_API_URL=wss://api.openai.com/v1/realtime
OPENAI_REALTIME_MODEL=gpt-4o-realtime-preview  # or gpt-realtime for production
OPENAI_REALTIME_VOICE=alloy  # Options: alloy, echo, fable, onyx, nova, shimmer

# OpenAI Advanced Features
OPENAI_ENABLE_STREAMING=true  # Stream chat responses for better UX
OPENAI_USE_STRUCTURED_OUTPUTS=true  # Guarantee valid JSON responses
OPENAI_USE_RESPONSES=false  # Use newer Responses API (experimental)

### Twilio
# IMPORTANT: Account SID must start with AC...
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_PHONE_NUMBER=+15551234567
TWILIO_WEBHOOK_URL=http://localhost:8000

# Voice-to-voice over Media Streams (needs a WS-capable host)
TWILIO_MEDIA_STREAMS_ENABLED=false
# Example: wss://xxxxx.ngrok.app  (must be publicly reachable by Twilio)
TWILIO_MEDIA_STREAMS_WS_BASE_URL=

### Database (Neon optional; will fall back to sqlite if empty/driver missing)
DATABASE_URL=

### Godfather identity (optional)
GODFATHER_PHONE_NUMBERS=+15551234567
GODFATHER_EMAIL=

### Auto-execute mode
# When true, AI executes calls/SMS/emails/calendar actions immediately without asking for confirmation
# Set to false to require manual approval for high-risk actions
AUTO_EXECUTE_HIGH_RISK=true

### Google Calendar OAuth (optional)
GOOGLE_OAUTH_CLIENT_SECRETS_FILE=
GOOGLE_OAUTH_CLIENT_SECRETS_JSON=
GOOGLE_OAUTH_TOKEN_FILE=secrets/google_token.json
GOOGLE_CALENDAR_ID=primary

### Gmail OAuth (optional)
GMAIL_OAUTH_CLIENT_SECRETS_FILE=
GMAIL_OAUTH_CLIENT_SECRETS_JSON=
GMAIL_OAUTH_TOKEN_FILE=secrets/gmail_token.json

### Outlook / Microsoft Graph OAuth (optional)
OUTLOOK_OAUTH_CLIENT_SECRETS_FILE=
OUTLOOK_OAUTH_CLIENT_SECRETS_JSON=
OUTLOOK_OAUTH_TOKEN_FILE=secrets/outlook_token.json

### SMTP fallback (optional)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=


